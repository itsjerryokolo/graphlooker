import * as React from 'react';
import { NavbarProps } from '../../interface/props';

const Navbar: React.FunctionComponent<NavbarProps> = ({theme, toggleTheme}) => {

return (
<div>
    navbar
</div>
);

};

export default Navbar;