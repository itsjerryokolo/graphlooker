import * as React from 'react';
import { HomeProps } from '../../interface/props';

const Home: React.FunctionComponent<HomeProps> = ({theme}) => {

return (
<div>
    home
</div>
);

};

export default Home;